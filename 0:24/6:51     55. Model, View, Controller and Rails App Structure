We will look at folder in a standard Rails project.


Rails follows the MVC Pattern. (Model View Controller)

MVC creates seperation between presentation layers and business logic (backend) layer.


Model :


Views:
View are representaitons of a web application and htis can vary in differetn forms. in case we want to diplay a page in html, it is not shown directly in HTMl, it is shown in embeded ruby format so a file being displayed in ROR looks like home.html.er. Here ERB stands for embedded ruby

Controllers:

